{
  "district_id": {
    "description": "Unique district identifier",
    "unit": "id",
    "cluster": "identifier"
  },
  "district": {
    "description": "District name",
    "unit": "text",
    "cluster": "identifier"
  },
  "state": {
    "description": "State or Union Territory name",
    "unit": "text",
    "cluster": "identifier"
  },
  "households_surveyed": {
    "description": "Number of households surveyed in NFHS-5",
    "unit": "count",
    "cluster": "sample"
  },
  "women_interviewed": {
    "description": "Number of women aged 15-49 years interviewed",
    "unit": "count",
    "cluster": "sample"
  },
  "men_interviewed": {
    "description": "Number of men aged 15-54 years interviewed",
    "unit": "count",
    "cluster": "sample"
  },
  "female_school_attendance_pct": {
    "description": "% of female population age 6+ who ever attended school",
    "unit": "percent",
    "cluster": "women_empowerment"
  },
  "pop_below_15_pct": {
    "description": "% of population below age 15 years",
    "unit": "percent",
    "cluster": "demography"
  },
  "sex_ratio": {
    "description": "Females per 1,000 males in total population",
    "unit": "ratio",
    "cluster": "demography"
  },
  "sex_ratio_at_birth": {
    "description": "Females per 1,000 males among children born in last 5 years",
    "unit": "ratio",
    "cluster": "demography"
  },
  "birth_registration_pct": {
    "description": "% of children under 5 whose birth was registered",
    "unit": "percent",
    "cluster": "demography"
  },
  "death_registration_pct": {
    "description": "death registration pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "electricity_access_pct": {
    "description": "% of population with electricity access",
    "unit": "percent",
    "cluster": "sanitation",
    "indicator_cluster": "sanitation"
  },
  "clean_water_access_pct": {
    "description": "% with improved drinking water source",
    "unit": "percent",
    "cluster": "sanitation",
    "indicator_cluster": "sanitation"
  },
  "improved_sanitation_pct": {
    "description": "% using improved sanitation facility",
    "unit": "percent",
    "cluster": "sanitation",
    "indicator_cluster": "sanitation"
  },
  "clean_cooking_fuel_pct": {
    "description": "% using clean fuel for cooking",
    "unit": "percent",
    "cluster": "sanitation",
    "indicator_cluster": "sanitation"
  },
  "iodized_salt_pct": {
    "description": "% of households using iodized salt",
    "unit": "percent",
    "cluster": "nutrition"
  },
  "health_insurance_pct": {
    "description": "% of households with health insurance coverage",
    "unit": "percent",
    "cluster": "healthcare_access"
  },
  "preprimary_school_pct": {
    "description": "preprimary school pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "women_literacy_pct": {
    "description": "% of women aged 15-49 who are literate",
    "unit": "percent",
    "cluster": "women_empowerment",
    "indicator_cluster": "women_empowerment"
  },
  "women_10yr_school_pct": {
    "description": "women 10yr school pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "child_marriage_pct": {
    "description": "% of women aged 20-24 married before age 18 (child marriage rate)",
    "unit": "percent",
    "cluster": "women_empowerment"
  },
  "births_3rd_plus_order_pct": {
    "description": "births 3rd plus order pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "teen_pregnancy_pct": {
    "description": "% of women aged 15-19 who are already mothers or pregnant",
    "unit": "percent",
    "cluster": "maternal_health"
  },
  "menstrual_hygiene_pct": {
    "description": "% of women 15-24 using hygienic menstrual protection methods",
    "unit": "percent",
    "cluster": "women_empowerment"
  },
  "fp_any_method_pct": {
    "description": "fp any method pct",
    "unit": "unknown",
    "cluster": "other",
    "indicator_cluster": "family_planning"
  },
  "fp_modern_method_pct": {
    "description": "% of currently married women using modern family planning methods",
    "unit": "percent",
    "cluster": "family_planning",
    "indicator_cluster": "family_planning"
  },
  "fp_female_sterilization_pct": {
    "description": "fp female sterilization pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_male_sterilization_pct": {
    "description": "fp male sterilization pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_iud_pct": {
    "description": "fp iud pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_pill_pct": {
    "description": "fp pill pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_condom_pct": {
    "description": "fp condom pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_injectables_pct": {
    "description": "fp injectables pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "unmet_fp_need_pct": {
    "description": "% of married women with unmet need for family planning",
    "unit": "percent",
    "cluster": "family_planning",
    "indicator_cluster": "family_planning"
  },
  "unmet_fp_spacing_pct": {
    "description": "unmet fp spacing pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_counselling_pct": {
    "description": "fp counselling pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fp_side_effects_counselled_pct": {
    "description": "fp side effects counselled pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "anc_first_trimester_pct": {
    "description": "% of mothers with antenatal check-up in first trimester",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "anc_4plus_visits_pct": {
    "description": "% of mothers with 4 or more antenatal care visits",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "tetanus_protection_pct": {
    "description": "tetanus protection pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "ifa_100days_pct": {
    "description": "% of mothers who consumed IFA tablets for 100+ days during pregnancy",
    "unit": "percent",
    "cluster": "maternal_health"
  },
  "ifa_180days_pct": {
    "description": "ifa 180days pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "mcp_card_pct": {
    "description": "mcp card pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "postnatal_care_mother_pct": {
    "description": "% of mothers receiving postnatal care within 2 days",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "oop_delivery_cost_rs": {
    "description": "Average out-of-pocket delivery expenditure in public facility (Rs.)",
    "unit": "rupees",
    "cluster": "maternal_health"
  },
  "home_birth_facility_checkup_pct": {
    "description": "home birth facility checkup pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "postnatal_care_child_pct": {
    "description": "postnatal care child pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "institutional_delivery_pct": {
    "description": "% of births occurring in a health facility (institutional delivery)",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "institutional_delivery_public_pct": {
    "description": "% of births in public health facilities",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "skilled_home_birth_pct": {
    "description": "skilled home birth pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "skilled_birth_attendant_pct": {
    "description": "% of births attended by skilled health personnel",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "csection_pct": {
    "description": "% of births by caesarean section",
    "unit": "percent",
    "cluster": "maternal_health",
    "indicator_cluster": "maternal_health"
  },
  "csection_private_pct": {
    "description": "csection private pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "csection_public_pct": {
    "description": "csection public pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "fully_vaccinated_recall_pct": {
    "description": "% of children 12-23 months fully vaccinated (card or recall)",
    "unit": "percent",
    "cluster": "vaccination",
    "indicator_cluster": "vaccination"
  },
  "fully_vaccinated_card_pct": {
    "description": "% of children 12-23 months fully vaccinated (card only)",
    "unit": "percent",
    "cluster": "vaccination",
    "indicator_cluster": "vaccination"
  },
  "bcg_pct": {
    "description": "% of children 12-23 months who received BCG vaccine",
    "unit": "percent",
    "cluster": "vaccination",
    "indicator_cluster": "vaccination"
  },
  "dpt3_pct": {
    "description": "% of children 12-23 months who received 3 doses of DPT vaccine",
    "unit": "percent",
    "cluster": "vaccination",
    "indicator_cluster": "vaccination"
  },
  "measles_pct": {
    "description": "% of children 12-23 months who received measles vaccine",
    "unit": "percent",
    "cluster": "vaccination",
    "indicator_cluster": "vaccination"
  },
  "hepb3_pct": {
    "description": "% of children 12-23 months who received 3 doses of Hepatitis B vaccine",
    "unit": "percent",
    "cluster": "vaccination",
    "indicator_cluster": "vaccination"
  },
  "vaccinated_in_public_pct": {
    "description": "vaccinated in public pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "vaccinated_in_private_pct": {
    "description": "vaccinated in private pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "early_breastfeed_pct": {
    "description": "% of children breastfed within 1 hour of birth",
    "unit": "percent",
    "cluster": "child_nutrition"
  },
  "exclusive_breastfeed_pct": {
    "description": "% of infants under 6 months exclusively breastfed",
    "unit": "percent",
    "cluster": "child_nutrition"
  },
  "complementary_feed_pct": {
    "description": "complementary feed pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "adequate_diet_breastfed_pct": {
    "description": "adequate diet breastfed pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "adequate_diet_nonbreastfed_pct": {
    "description": "adequate diet nonbreastfed pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "adequate_diet_total_pct": {
    "description": "adequate diet total pct",
    "unit": "unknown",
    "cluster": "other"
  },
  "stunting_pct": {
    "description": "% of children under 5 who are stunted (low height-for-age, chronic malnutrition)",
    "unit": "percent",
    "cluster": "child_nutrition",
    "indicator_cluster": "child_nutrition"
  },
  "wasting_pct": {
    "description": "% of children under 5 who are wasted (low weight-for-height, acute malnutrition)",
    "unit": "percent",
    "cluster": "child_nutrition",
    "indicator_cluster": "child_nutrition"
  },
  "severe_wasting_pct": {
    "description": "% of children under 5 who are severely wasted",
    "unit": "percent",
    "cluster": "child_nutrition",
    "indicator_cluster": "child_nutrition"
  },
  "underweight_pct": {
    "description": "% of children under 5 who are underweight (low weight-for-age)",
    "unit": "percent",
    "cluster": "child_nutrition",
    "indicator_cluster": "child_nutrition"
  },
  "overweight_children_pct": {
    "description": "% of children under 5 who are overweight",
    "unit": "percent",
    "cluster": "child_nutrition",
    "indicator_cluster": "child_nutrition"
  },
  "women_low_bmi_pct": {
    "description": "% of women 15-49 with BMI below normal (<18.5)",
    "unit": "percent",
    "cluster": "adult_nutrition"
  },
  "anaemia_children_pct": {
    "description": "% of children 6-59 months who are anaemic (<11.0 g/dl)",
    "unit": "percent",
    "cluster": "anaemia",
    "indicator_cluster": "anaemia"
  },
  "anaemia_nonpregnant_women_pct": {
    "description": "% of non-pregnant women 15-49 who are anaemic (<12.0 g/dl)",
    "unit": "percent",
    "cluster": "anaemia",
    "indicator_cluster": "anaemia"
  },
  "anaemia_all_women_pct": {
    "description": "% of all women 15-49 who are anaemic",
    "unit": "percent",
    "cluster": "anaemia",
    "indicator_cluster": "anaemia"
  },
  "anaemia_men_pct": {
    "description": "% of men 15-49 who are anaemic (<13.0 g/dl)",
    "unit": "percent",
    "cluster": "anaemia",
    "indicator_cluster": "anaemia"
  },
  "stunting_pct_national_rank": {
    "description": "National rank for stunting_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "stunting_pct_national_percentile": {
    "description": "National percentile for stunting_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "wasting_pct_national_rank": {
    "description": "National rank for wasting_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "wasting_pct_national_percentile": {
    "description": "National percentile for wasting_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "underweight_pct_national_rank": {
    "description": "National rank for underweight_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "underweight_pct_national_percentile": {
    "description": "National percentile for underweight_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "anaemia_children_pct_national_rank": {
    "description": "National rank for anaemia_children_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "anaemia_children_pct_national_percentile": {
    "description": "National percentile for anaemia_children_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "anaemia_all_women_pct_national_rank": {
    "description": "National rank for anaemia_all_women_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "anaemia_all_women_pct_national_percentile": {
    "description": "National percentile for anaemia_all_women_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "institutional_delivery_pct_national_rank": {
    "description": "National rank for institutional_delivery_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "institutional_delivery_pct_national_percentile": {
    "description": "National percentile for institutional_delivery_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "fully_vaccinated_recall_pct_national_rank": {
    "description": "National rank for fully_vaccinated_recall_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "fully_vaccinated_recall_pct_national_percentile": {
    "description": "National percentile for fully_vaccinated_recall_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "improved_sanitation_pct_national_rank": {
    "description": "National rank for improved_sanitation_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "improved_sanitation_pct_national_percentile": {
    "description": "National percentile for improved_sanitation_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "anc_4plus_visits_pct_national_rank": {
    "description": "National rank for anc_4plus_visits_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "anc_4plus_visits_pct_national_percentile": {
    "description": "National percentile for anc_4plus_visits_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "women_literacy_pct_national_rank": {
    "description": "National rank for women_literacy_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "women_literacy_pct_national_percentile": {
    "description": "National percentile for women_literacy_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "child_marriage_pct_national_rank": {
    "description": "National rank for child_marriage_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "child_marriage_pct_national_percentile": {
    "description": "National percentile for child_marriage_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "unmet_fp_need_pct_national_rank": {
    "description": "National rank for unmet_fp_need_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "unmet_fp_need_pct_national_percentile": {
    "description": "National percentile for unmet_fp_need_pct (100=best performing)",
    "unit": "percentile",
    "cluster": "derived"
  },
  "stunting_pct_state_rank": {
    "description": "National rank for stunting_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "anaemia_children_pct_state_rank": {
    "description": "National rank for anaemia_children_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "institutional_delivery_pct_state_rank": {
    "description": "National rank for institutional_delivery_pct (1=worst district)",
    "unit": "rank",
    "cluster": "derived"
  },
  "child_health_score": {
    "description": "Composite child health score (0-100, 100=best)",
    "unit": "score",
    "cluster": "derived"
  },
  "maternal_health_score": {
    "description": "Composite maternal health score (0-100, 100=best)",
    "unit": "score",
    "cluster": "derived"
  },
  "female_school_attendance_pct_nfhs4_state": {
    "description": "% of female population age 6+ who ever attended school — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "pop_below_15_pct_nfhs4_state": {
    "description": "% of population below age 15 years — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "sex_ratio_nfhs4_state": {
    "description": "Females per 1,000 males in total population — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "ratio",
    "cluster": "trend"
  },
  "sex_ratio_at_birth_nfhs4_state": {
    "description": "Females per 1,000 males among children born in last 5 years — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "ratio",
    "cluster": "trend"
  },
  "birth_registration_pct_nfhs4_state": {
    "description": "% of children under 5 whose birth was registered — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "electricity_access_pct_nfhs4_state": {
    "description": "% of population with electricity access — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "clean_water_access_pct_nfhs4_state": {
    "description": "% with improved drinking water source — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "improved_sanitation_pct_nfhs4_state": {
    "description": "% using improved sanitation facility — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "clean_cooking_fuel_pct_nfhs4_state": {
    "description": "% using clean fuel for cooking — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "iodized_salt_pct_nfhs4_state": {
    "description": "% of households using iodized salt — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "health_insurance_pct_nfhs4_state": {
    "description": "% of households with health insurance coverage — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_literacy_pct_nfhs4_state": {
    "description": "% of women aged 15-49 who are literate — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_10yr_school_pct_nfhs4_state": {
    "description": "women 10yr school pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "child_marriage_pct_nfhs4_state": {
    "description": "% of women aged 20-24 married before age 18 (child marriage rate) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "teen_pregnancy_pct_nfhs4_state": {
    "description": "% of women aged 15-19 who are already mothers or pregnant — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "fp_any_method_pct_nfhs4_state": {
    "description": "fp any method pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_modern_method_pct_nfhs4_state": {
    "description": "% of currently married women using modern family planning methods — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "fp_female_sterilization_pct_nfhs4_state": {
    "description": "fp female sterilization pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_male_sterilization_pct_nfhs4_state": {
    "description": "fp male sterilization pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_iud_pct_nfhs4_state": {
    "description": "fp iud pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_pill_pct_nfhs4_state": {
    "description": "fp pill pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_condom_pct_nfhs4_state": {
    "description": "fp condom pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "unmet_fp_need_pct_nfhs4_state": {
    "description": "% of married women with unmet need for family planning — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "unmet_fp_spacing_pct_nfhs4_state": {
    "description": "unmet fp spacing pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_counselling_pct_nfhs4_state": {
    "description": "fp counselling pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "anc_first_trimester_pct_nfhs4_state": {
    "description": "% of mothers with antenatal check-up in first trimester — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anc_4plus_visits_pct_nfhs4_state": {
    "description": "% of mothers with 4 or more antenatal care visits — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "ifa_100days_pct_nfhs4_state": {
    "description": "% of mothers who consumed IFA tablets for 100+ days during pregnancy — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "mcp_card_pct_nfhs4_state": {
    "description": "mcp card pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "postnatal_care_mother_pct_nfhs4_state": {
    "description": "% of mothers receiving postnatal care within 2 days — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "oop_delivery_cost_rs_nfhs4_state": {
    "description": "Average out-of-pocket delivery expenditure in public facility (Rs.) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "rupees",
    "cluster": "trend"
  },
  "home_birth_facility_checkup_pct_nfhs4_state": {
    "description": "home birth facility checkup pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "postnatal_care_child_pct_nfhs4_state": {
    "description": "postnatal care child pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "institutional_delivery_pct_nfhs4_state": {
    "description": "% of births occurring in a health facility (institutional delivery) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "institutional_delivery_public_pct_nfhs4_state": {
    "description": "% of births in public health facilities — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "skilled_home_birth_pct_nfhs4_state": {
    "description": "skilled home birth pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "skilled_birth_attendant_pct_nfhs4_state": {
    "description": "% of births attended by skilled health personnel — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "csection_pct_nfhs4_state": {
    "description": "% of births by caesarean section — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "csection_private_pct_nfhs4_state": {
    "description": "csection private pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "csection_public_pct_nfhs4_state": {
    "description": "csection public pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "bcg_pct_nfhs4_state": {
    "description": "% of children 12-23 months who received BCG vaccine — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "polio3_pct_nfhs4_state": {
    "description": "polio3 pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "dpt3_pct_nfhs4_state": {
    "description": "% of children 12-23 months who received 3 doses of DPT vaccine — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "measles_pct_nfhs4_state": {
    "description": "% of children 12-23 months who received measles vaccine — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "hepb3_pct_nfhs4_state": {
    "description": "% of children 12-23 months who received 3 doses of Hepatitis B vaccine — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "vaccinated_in_public_pct_nfhs4_state": {
    "description": "vaccinated in public pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "vaccinated_in_private_pct_nfhs4_state": {
    "description": "vaccinated in private pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "diarrhoea_ors_pct_nfhs4_state": {
    "description": "diarrhoea ors pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "diarrhoea_zinc_pct_nfhs4_state": {
    "description": "diarrhoea zinc pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "diarrhoea_facility_pct_nfhs4_state": {
    "description": "diarrhoea facility pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "ari_facility_pct_nfhs4_state": {
    "description": "ari facility pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "early_breastfeed_pct_nfhs4_state": {
    "description": "% of children breastfed within 1 hour of birth — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "exclusive_breastfeed_pct_nfhs4_state": {
    "description": "% of infants under 6 months exclusively breastfed — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "complementary_feed_pct_nfhs4_state": {
    "description": "complementary feed pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_breastfed_pct_nfhs4_state": {
    "description": "adequate diet breastfed pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_nonbreastfed_pct_nfhs4_state": {
    "description": "adequate diet nonbreastfed pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_total_pct_nfhs4_state": {
    "description": "adequate diet total pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "stunting_pct_nfhs4_state": {
    "description": "% of children under 5 who are stunted (low height-for-age, chronic malnutrition) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "wasting_pct_nfhs4_state": {
    "description": "% of children under 5 who are wasted (low weight-for-height, acute malnutrition) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "severe_wasting_pct_nfhs4_state": {
    "description": "% of children under 5 who are severely wasted — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "underweight_pct_nfhs4_state": {
    "description": "% of children under 5 who are underweight (low weight-for-age) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_low_bmi_pct_nfhs4_state": {
    "description": "% of women 15-49 with BMI below normal (<18.5) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_low_bmi_pct_nfhs4_state": {
    "description": "men low bmi pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_overweight_pct_nfhs4_state": {
    "description": "women overweight pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_overweight_pct_nfhs4_state": {
    "description": "men overweight pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_children_pct_nfhs4_state": {
    "description": "% of children 6-59 months who are anaemic (<11.0 g/dl) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_nonpregnant_women_pct_nfhs4_state": {
    "description": "% of non-pregnant women 15-49 who are anaemic (<12.0 g/dl) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_pregnant_women_pct_nfhs4_state": {
    "description": "anaemia pregnant women pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_all_women_pct_nfhs4_state": {
    "description": "% of all women 15-49 who are anaemic — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_men_pct_nfhs4_state": {
    "description": "% of men 15-49 who are anaemic (<13.0 g/dl) — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_high_blood_sugar_pct_nfhs4_state": {
    "description": "women high blood sugar pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_high_blood_sugar_pct_nfhs4_state": {
    "description": "men high blood sugar pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_hypertension_mild_pct_nfhs4_state": {
    "description": "women hypertension mild pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_hypertension_mild_pct_nfhs4_state": {
    "description": "men hypertension mild pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_hypertension_severe_pct_nfhs4_state": {
    "description": "women hypertension severe pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_hypertension_severe_pct_nfhs4_state": {
    "description": "men hypertension severe pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "cervix_exam_pct_nfhs4_state": {
    "description": "cervix exam pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "breast_exam_pct_nfhs4_state": {
    "description": "breast exam pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "oral_exam_pct_nfhs4_state": {
    "description": "oral exam pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_hiv_knowledge_pct_nfhs4_state": {
    "description": "women hiv knowledge pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_hiv_knowledge_pct_nfhs4_state": {
    "description": "men hiv knowledge pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_condom_hiv_knowledge_pct_nfhs4_state": {
    "description": "women condom hiv knowledge pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_condom_hiv_knowledge_pct_nfhs4_state": {
    "description": "men condom hiv knowledge pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_household_decisions_pct_nfhs4_state": {
    "description": "women household decisions pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_paid_work_pct_nfhs4_state": {
    "description": "women paid work pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "spousal_violence_pct_nfhs4_state": {
    "description": "spousal violence pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "pregnancy_violence_pct_nfhs4_state": {
    "description": "pregnancy violence pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_property_ownership_pct_nfhs4_state": {
    "description": "women property ownership pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_bank_account_pct_nfhs4_state": {
    "description": "women bank account pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_mobile_phone_pct_nfhs4_state": {
    "description": "women mobile phone pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "menstrual_hygiene_pct_nfhs4_state": {
    "description": "% of women 15-24 using hygienic menstrual protection methods — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_tobacco_pct_nfhs4_state": {
    "description": "men tobacco pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "men_alcohol_pct_nfhs4_state": {
    "description": "men alcohol pct — NFHS-4 (2015-16) STATE-level baseline (district's parent state average, not district-level)",
    "unit": "percent",
    "cluster": "trend"
  },
  "female_school_attendance_pct_change_from_nfhs4": {
    "description": "% of female population age 6+ who ever attended school — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "female_school_attendance_pct_change_pct_from_nfhs4": {
    "description": "% of female population age 6+ who ever attended school — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "female_school_attendance_pct_filled": {
    "description": "% of female population age 6+ who ever attended school — same as `female_school_attendance_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `female_school_attendance_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "female_school_attendance_pct_is_imputed": {
    "description": "True if `female_school_attendance_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `female_school_attendance_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "pop_below_15_pct_change_from_nfhs4": {
    "description": "% of population below age 15 years — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "pop_below_15_pct_change_pct_from_nfhs4": {
    "description": "% of population below age 15 years — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "pop_below_15_pct_filled": {
    "description": "% of population below age 15 years — same as `pop_below_15_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `pop_below_15_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "pop_below_15_pct_is_imputed": {
    "description": "True if `pop_below_15_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `pop_below_15_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "sex_ratio_change_from_nfhs4": {
    "description": "Females per 1,000 males in total population — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "ratio",
    "cluster": "trend"
  },
  "sex_ratio_change_pct_from_nfhs4": {
    "description": "Females per 1,000 males in total population — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "sex_ratio_filled": {
    "description": "Females per 1,000 males in total population — same as `sex_ratio`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `sex_ratio_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "ratio",
    "cluster": "trend"
  },
  "sex_ratio_is_imputed": {
    "description": "True if `sex_ratio_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `sex_ratio` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "sex_ratio_at_birth_change_from_nfhs4": {
    "description": "Females per 1,000 males among children born in last 5 years — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "ratio",
    "cluster": "trend"
  },
  "sex_ratio_at_birth_change_pct_from_nfhs4": {
    "description": "Females per 1,000 males among children born in last 5 years — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "sex_ratio_at_birth_filled": {
    "description": "Females per 1,000 males among children born in last 5 years — same as `sex_ratio_at_birth`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `sex_ratio_at_birth_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "ratio",
    "cluster": "trend"
  },
  "sex_ratio_at_birth_is_imputed": {
    "description": "True if `sex_ratio_at_birth_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `sex_ratio_at_birth` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "birth_registration_pct_change_from_nfhs4": {
    "description": "% of children under 5 whose birth was registered — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "birth_registration_pct_change_pct_from_nfhs4": {
    "description": "% of children under 5 whose birth was registered — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "birth_registration_pct_filled": {
    "description": "% of children under 5 whose birth was registered — same as `birth_registration_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `birth_registration_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "birth_registration_pct_is_imputed": {
    "description": "True if `birth_registration_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `birth_registration_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "electricity_access_pct_change_from_nfhs4": {
    "description": "% of population with electricity access — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "electricity_access_pct_change_pct_from_nfhs4": {
    "description": "% of population with electricity access — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "electricity_access_pct_filled": {
    "description": "% of population with electricity access — same as `electricity_access_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `electricity_access_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "electricity_access_pct_is_imputed": {
    "description": "True if `electricity_access_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `electricity_access_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "clean_water_access_pct_change_from_nfhs4": {
    "description": "% with improved drinking water source — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "clean_water_access_pct_change_pct_from_nfhs4": {
    "description": "% with improved drinking water source — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "clean_water_access_pct_filled": {
    "description": "% with improved drinking water source — same as `clean_water_access_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `clean_water_access_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "clean_water_access_pct_is_imputed": {
    "description": "True if `clean_water_access_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `clean_water_access_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "improved_sanitation_pct_change_from_nfhs4": {
    "description": "% using improved sanitation facility — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "improved_sanitation_pct_change_pct_from_nfhs4": {
    "description": "% using improved sanitation facility — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "improved_sanitation_pct_filled": {
    "description": "% using improved sanitation facility — same as `improved_sanitation_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `improved_sanitation_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "improved_sanitation_pct_is_imputed": {
    "description": "True if `improved_sanitation_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `improved_sanitation_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "clean_cooking_fuel_pct_change_from_nfhs4": {
    "description": "% using clean fuel for cooking — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "clean_cooking_fuel_pct_change_pct_from_nfhs4": {
    "description": "% using clean fuel for cooking — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "clean_cooking_fuel_pct_filled": {
    "description": "% using clean fuel for cooking — same as `clean_cooking_fuel_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `clean_cooking_fuel_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "clean_cooking_fuel_pct_is_imputed": {
    "description": "True if `clean_cooking_fuel_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `clean_cooking_fuel_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "iodized_salt_pct_change_from_nfhs4": {
    "description": "% of households using iodized salt — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "iodized_salt_pct_change_pct_from_nfhs4": {
    "description": "% of households using iodized salt — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "iodized_salt_pct_filled": {
    "description": "% of households using iodized salt — same as `iodized_salt_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `iodized_salt_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "iodized_salt_pct_is_imputed": {
    "description": "True if `iodized_salt_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `iodized_salt_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "health_insurance_pct_change_from_nfhs4": {
    "description": "% of households with health insurance coverage — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "health_insurance_pct_change_pct_from_nfhs4": {
    "description": "% of households with health insurance coverage — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "health_insurance_pct_filled": {
    "description": "% of households with health insurance coverage — same as `health_insurance_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `health_insurance_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "health_insurance_pct_is_imputed": {
    "description": "True if `health_insurance_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `health_insurance_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "women_literacy_pct_change_from_nfhs4": {
    "description": "% of women aged 15-49 who are literate — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_literacy_pct_change_pct_from_nfhs4": {
    "description": "% of women aged 15-49 who are literate — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "women_literacy_pct_filled": {
    "description": "% of women aged 15-49 who are literate — same as `women_literacy_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `women_literacy_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_literacy_pct_is_imputed": {
    "description": "True if `women_literacy_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `women_literacy_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "women_10yr_school_pct_change_from_nfhs4": {
    "description": "women 10yr school pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "women_10yr_school_pct_change_pct_from_nfhs4": {
    "description": "women 10yr school pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "women_10yr_school_pct_filled": {
    "description": "women 10yr school pct — same as `women_10yr_school_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `women_10yr_school_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "women_10yr_school_pct_is_imputed": {
    "description": "True if `women_10yr_school_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `women_10yr_school_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "child_marriage_pct_change_from_nfhs4": {
    "description": "% of women aged 20-24 married before age 18 (child marriage rate) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "child_marriage_pct_change_pct_from_nfhs4": {
    "description": "% of women aged 20-24 married before age 18 (child marriage rate) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "child_marriage_pct_filled": {
    "description": "% of women aged 20-24 married before age 18 (child marriage rate) — same as `child_marriage_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `child_marriage_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "child_marriage_pct_is_imputed": {
    "description": "True if `child_marriage_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `child_marriage_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "teen_pregnancy_pct_change_from_nfhs4": {
    "description": "% of women aged 15-19 who are already mothers or pregnant — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "teen_pregnancy_pct_change_pct_from_nfhs4": {
    "description": "% of women aged 15-19 who are already mothers or pregnant — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "teen_pregnancy_pct_filled": {
    "description": "% of women aged 15-19 who are already mothers or pregnant — same as `teen_pregnancy_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `teen_pregnancy_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "teen_pregnancy_pct_is_imputed": {
    "description": "True if `teen_pregnancy_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `teen_pregnancy_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_any_method_pct_change_from_nfhs4": {
    "description": "fp any method pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_any_method_pct_change_pct_from_nfhs4": {
    "description": "fp any method pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_any_method_pct_filled": {
    "description": "fp any method pct — same as `fp_any_method_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_any_method_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_any_method_pct_is_imputed": {
    "description": "True if `fp_any_method_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_any_method_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_modern_method_pct_change_from_nfhs4": {
    "description": "% of currently married women using modern family planning methods — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "fp_modern_method_pct_change_pct_from_nfhs4": {
    "description": "% of currently married women using modern family planning methods — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_modern_method_pct_filled": {
    "description": "% of currently married women using modern family planning methods — same as `fp_modern_method_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_modern_method_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "fp_modern_method_pct_is_imputed": {
    "description": "True if `fp_modern_method_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_modern_method_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_female_sterilization_pct_change_from_nfhs4": {
    "description": "fp female sterilization pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_female_sterilization_pct_change_pct_from_nfhs4": {
    "description": "fp female sterilization pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_female_sterilization_pct_filled": {
    "description": "fp female sterilization pct — same as `fp_female_sterilization_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_female_sterilization_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_female_sterilization_pct_is_imputed": {
    "description": "True if `fp_female_sterilization_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_female_sterilization_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_male_sterilization_pct_change_from_nfhs4": {
    "description": "fp male sterilization pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_male_sterilization_pct_change_pct_from_nfhs4": {
    "description": "fp male sterilization pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_male_sterilization_pct_filled": {
    "description": "fp male sterilization pct — same as `fp_male_sterilization_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_male_sterilization_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_male_sterilization_pct_is_imputed": {
    "description": "True if `fp_male_sterilization_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_male_sterilization_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_iud_pct_change_from_nfhs4": {
    "description": "fp iud pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_iud_pct_change_pct_from_nfhs4": {
    "description": "fp iud pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_iud_pct_filled": {
    "description": "fp iud pct — same as `fp_iud_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_iud_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_iud_pct_is_imputed": {
    "description": "True if `fp_iud_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_iud_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_pill_pct_change_from_nfhs4": {
    "description": "fp pill pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_pill_pct_change_pct_from_nfhs4": {
    "description": "fp pill pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_pill_pct_filled": {
    "description": "fp pill pct — same as `fp_pill_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_pill_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_pill_pct_is_imputed": {
    "description": "True if `fp_pill_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_pill_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_condom_pct_change_from_nfhs4": {
    "description": "fp condom pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_condom_pct_change_pct_from_nfhs4": {
    "description": "fp condom pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_condom_pct_filled": {
    "description": "fp condom pct — same as `fp_condom_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_condom_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_condom_pct_is_imputed": {
    "description": "True if `fp_condom_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_condom_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "unmet_fp_need_pct_change_from_nfhs4": {
    "description": "% of married women with unmet need for family planning — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "unmet_fp_need_pct_change_pct_from_nfhs4": {
    "description": "% of married women with unmet need for family planning — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "unmet_fp_need_pct_filled": {
    "description": "% of married women with unmet need for family planning — same as `unmet_fp_need_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `unmet_fp_need_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "unmet_fp_need_pct_is_imputed": {
    "description": "True if `unmet_fp_need_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `unmet_fp_need_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "unmet_fp_spacing_pct_change_from_nfhs4": {
    "description": "unmet fp spacing pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "unmet_fp_spacing_pct_change_pct_from_nfhs4": {
    "description": "unmet fp spacing pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "unmet_fp_spacing_pct_filled": {
    "description": "unmet fp spacing pct — same as `unmet_fp_spacing_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `unmet_fp_spacing_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "unmet_fp_spacing_pct_is_imputed": {
    "description": "True if `unmet_fp_spacing_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `unmet_fp_spacing_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "fp_counselling_pct_change_from_nfhs4": {
    "description": "fp counselling pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_counselling_pct_change_pct_from_nfhs4": {
    "description": "fp counselling pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "fp_counselling_pct_filled": {
    "description": "fp counselling pct — same as `fp_counselling_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `fp_counselling_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "fp_counselling_pct_is_imputed": {
    "description": "True if `fp_counselling_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `fp_counselling_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "anc_first_trimester_pct_change_from_nfhs4": {
    "description": "% of mothers with antenatal check-up in first trimester — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anc_first_trimester_pct_change_pct_from_nfhs4": {
    "description": "% of mothers with antenatal check-up in first trimester — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "anc_first_trimester_pct_filled": {
    "description": "% of mothers with antenatal check-up in first trimester — same as `anc_first_trimester_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `anc_first_trimester_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "anc_first_trimester_pct_is_imputed": {
    "description": "True if `anc_first_trimester_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `anc_first_trimester_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "anc_4plus_visits_pct_change_from_nfhs4": {
    "description": "% of mothers with 4 or more antenatal care visits — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anc_4plus_visits_pct_change_pct_from_nfhs4": {
    "description": "% of mothers with 4 or more antenatal care visits — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "anc_4plus_visits_pct_filled": {
    "description": "% of mothers with 4 or more antenatal care visits — same as `anc_4plus_visits_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `anc_4plus_visits_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "anc_4plus_visits_pct_is_imputed": {
    "description": "True if `anc_4plus_visits_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `anc_4plus_visits_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "ifa_100days_pct_change_from_nfhs4": {
    "description": "% of mothers who consumed IFA tablets for 100+ days during pregnancy — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "ifa_100days_pct_change_pct_from_nfhs4": {
    "description": "% of mothers who consumed IFA tablets for 100+ days during pregnancy — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "ifa_100days_pct_filled": {
    "description": "% of mothers who consumed IFA tablets for 100+ days during pregnancy — same as `ifa_100days_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `ifa_100days_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "ifa_100days_pct_is_imputed": {
    "description": "True if `ifa_100days_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `ifa_100days_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "mcp_card_pct_change_from_nfhs4": {
    "description": "mcp card pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "mcp_card_pct_change_pct_from_nfhs4": {
    "description": "mcp card pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "mcp_card_pct_filled": {
    "description": "mcp card pct — same as `mcp_card_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `mcp_card_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "mcp_card_pct_is_imputed": {
    "description": "True if `mcp_card_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `mcp_card_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "postnatal_care_mother_pct_change_from_nfhs4": {
    "description": "% of mothers receiving postnatal care within 2 days — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "postnatal_care_mother_pct_change_pct_from_nfhs4": {
    "description": "% of mothers receiving postnatal care within 2 days — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "postnatal_care_mother_pct_filled": {
    "description": "% of mothers receiving postnatal care within 2 days — same as `postnatal_care_mother_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `postnatal_care_mother_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "postnatal_care_mother_pct_is_imputed": {
    "description": "True if `postnatal_care_mother_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `postnatal_care_mother_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "oop_delivery_cost_rs_change_from_nfhs4": {
    "description": "Average out-of-pocket delivery expenditure in public facility (Rs.) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "rupees",
    "cluster": "trend"
  },
  "oop_delivery_cost_rs_change_pct_from_nfhs4": {
    "description": "Average out-of-pocket delivery expenditure in public facility (Rs.) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "oop_delivery_cost_rs_filled": {
    "description": "Average out-of-pocket delivery expenditure in public facility (Rs.) — same as `oop_delivery_cost_rs`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `oop_delivery_cost_rs_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "rupees",
    "cluster": "trend"
  },
  "oop_delivery_cost_rs_is_imputed": {
    "description": "True if `oop_delivery_cost_rs_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `oop_delivery_cost_rs` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "home_birth_facility_checkup_pct_change_from_nfhs4": {
    "description": "home birth facility checkup pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "home_birth_facility_checkup_pct_change_pct_from_nfhs4": {
    "description": "home birth facility checkup pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "home_birth_facility_checkup_pct_filled": {
    "description": "home birth facility checkup pct — same as `home_birth_facility_checkup_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `home_birth_facility_checkup_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "home_birth_facility_checkup_pct_is_imputed": {
    "description": "True if `home_birth_facility_checkup_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `home_birth_facility_checkup_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "postnatal_care_child_pct_change_from_nfhs4": {
    "description": "postnatal care child pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "postnatal_care_child_pct_change_pct_from_nfhs4": {
    "description": "postnatal care child pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "postnatal_care_child_pct_filled": {
    "description": "postnatal care child pct — same as `postnatal_care_child_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `postnatal_care_child_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "postnatal_care_child_pct_is_imputed": {
    "description": "True if `postnatal_care_child_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `postnatal_care_child_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "institutional_delivery_pct_change_from_nfhs4": {
    "description": "% of births occurring in a health facility (institutional delivery) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "institutional_delivery_pct_change_pct_from_nfhs4": {
    "description": "% of births occurring in a health facility (institutional delivery) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "institutional_delivery_pct_filled": {
    "description": "% of births occurring in a health facility (institutional delivery) — same as `institutional_delivery_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `institutional_delivery_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "institutional_delivery_pct_is_imputed": {
    "description": "True if `institutional_delivery_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `institutional_delivery_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "institutional_delivery_public_pct_change_from_nfhs4": {
    "description": "% of births in public health facilities — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "institutional_delivery_public_pct_change_pct_from_nfhs4": {
    "description": "% of births in public health facilities — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "institutional_delivery_public_pct_filled": {
    "description": "% of births in public health facilities — same as `institutional_delivery_public_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `institutional_delivery_public_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "institutional_delivery_public_pct_is_imputed": {
    "description": "True if `institutional_delivery_public_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `institutional_delivery_public_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "skilled_home_birth_pct_change_from_nfhs4": {
    "description": "skilled home birth pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "skilled_home_birth_pct_change_pct_from_nfhs4": {
    "description": "skilled home birth pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "skilled_home_birth_pct_filled": {
    "description": "skilled home birth pct — same as `skilled_home_birth_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `skilled_home_birth_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "skilled_home_birth_pct_is_imputed": {
    "description": "True if `skilled_home_birth_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `skilled_home_birth_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "skilled_birth_attendant_pct_change_from_nfhs4": {
    "description": "% of births attended by skilled health personnel — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "skilled_birth_attendant_pct_change_pct_from_nfhs4": {
    "description": "% of births attended by skilled health personnel — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "skilled_birth_attendant_pct_filled": {
    "description": "% of births attended by skilled health personnel — same as `skilled_birth_attendant_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `skilled_birth_attendant_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "skilled_birth_attendant_pct_is_imputed": {
    "description": "True if `skilled_birth_attendant_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `skilled_birth_attendant_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "csection_pct_change_from_nfhs4": {
    "description": "% of births by caesarean section — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "csection_pct_change_pct_from_nfhs4": {
    "description": "% of births by caesarean section — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "csection_pct_filled": {
    "description": "% of births by caesarean section — same as `csection_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `csection_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "csection_pct_is_imputed": {
    "description": "True if `csection_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `csection_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "csection_private_pct_change_from_nfhs4": {
    "description": "csection private pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "csection_private_pct_change_pct_from_nfhs4": {
    "description": "csection private pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "csection_private_pct_filled": {
    "description": "csection private pct — same as `csection_private_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `csection_private_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "csection_private_pct_is_imputed": {
    "description": "True if `csection_private_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `csection_private_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "csection_public_pct_change_from_nfhs4": {
    "description": "csection public pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "csection_public_pct_change_pct_from_nfhs4": {
    "description": "csection public pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "csection_public_pct_filled": {
    "description": "csection public pct — same as `csection_public_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `csection_public_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "csection_public_pct_is_imputed": {
    "description": "True if `csection_public_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `csection_public_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "bcg_pct_change_from_nfhs4": {
    "description": "% of children 12-23 months who received BCG vaccine — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "bcg_pct_change_pct_from_nfhs4": {
    "description": "% of children 12-23 months who received BCG vaccine — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "bcg_pct_filled": {
    "description": "% of children 12-23 months who received BCG vaccine — same as `bcg_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `bcg_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "bcg_pct_is_imputed": {
    "description": "True if `bcg_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `bcg_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "dpt3_pct_change_from_nfhs4": {
    "description": "% of children 12-23 months who received 3 doses of DPT vaccine — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "dpt3_pct_change_pct_from_nfhs4": {
    "description": "% of children 12-23 months who received 3 doses of DPT vaccine — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "dpt3_pct_filled": {
    "description": "% of children 12-23 months who received 3 doses of DPT vaccine — same as `dpt3_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `dpt3_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "dpt3_pct_is_imputed": {
    "description": "True if `dpt3_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `dpt3_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "measles_pct_change_from_nfhs4": {
    "description": "% of children 12-23 months who received measles vaccine — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "measles_pct_change_pct_from_nfhs4": {
    "description": "% of children 12-23 months who received measles vaccine — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "measles_pct_filled": {
    "description": "% of children 12-23 months who received measles vaccine — same as `measles_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `measles_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "measles_pct_is_imputed": {
    "description": "True if `measles_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `measles_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "hepb3_pct_change_from_nfhs4": {
    "description": "% of children 12-23 months who received 3 doses of Hepatitis B vaccine — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "hepb3_pct_change_pct_from_nfhs4": {
    "description": "% of children 12-23 months who received 3 doses of Hepatitis B vaccine — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "hepb3_pct_filled": {
    "description": "% of children 12-23 months who received 3 doses of Hepatitis B vaccine — same as `hepb3_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `hepb3_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "hepb3_pct_is_imputed": {
    "description": "True if `hepb3_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `hepb3_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "vaccinated_in_public_pct_change_from_nfhs4": {
    "description": "vaccinated in public pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "vaccinated_in_public_pct_change_pct_from_nfhs4": {
    "description": "vaccinated in public pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "vaccinated_in_public_pct_filled": {
    "description": "vaccinated in public pct — same as `vaccinated_in_public_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `vaccinated_in_public_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "vaccinated_in_public_pct_is_imputed": {
    "description": "True if `vaccinated_in_public_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `vaccinated_in_public_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "vaccinated_in_private_pct_change_from_nfhs4": {
    "description": "vaccinated in private pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "vaccinated_in_private_pct_change_pct_from_nfhs4": {
    "description": "vaccinated in private pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "vaccinated_in_private_pct_filled": {
    "description": "vaccinated in private pct — same as `vaccinated_in_private_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `vaccinated_in_private_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "vaccinated_in_private_pct_is_imputed": {
    "description": "True if `vaccinated_in_private_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `vaccinated_in_private_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "early_breastfeed_pct_change_from_nfhs4": {
    "description": "% of children breastfed within 1 hour of birth — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "early_breastfeed_pct_change_pct_from_nfhs4": {
    "description": "% of children breastfed within 1 hour of birth — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "early_breastfeed_pct_filled": {
    "description": "% of children breastfed within 1 hour of birth — same as `early_breastfeed_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `early_breastfeed_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "early_breastfeed_pct_is_imputed": {
    "description": "True if `early_breastfeed_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `early_breastfeed_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "exclusive_breastfeed_pct_change_from_nfhs4": {
    "description": "% of infants under 6 months exclusively breastfed — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "exclusive_breastfeed_pct_change_pct_from_nfhs4": {
    "description": "% of infants under 6 months exclusively breastfed — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "exclusive_breastfeed_pct_filled": {
    "description": "% of infants under 6 months exclusively breastfed — same as `exclusive_breastfeed_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `exclusive_breastfeed_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "exclusive_breastfeed_pct_is_imputed": {
    "description": "True if `exclusive_breastfeed_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `exclusive_breastfeed_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "complementary_feed_pct_change_from_nfhs4": {
    "description": "complementary feed pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "complementary_feed_pct_change_pct_from_nfhs4": {
    "description": "complementary feed pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "complementary_feed_pct_filled": {
    "description": "complementary feed pct — same as `complementary_feed_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `complementary_feed_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "complementary_feed_pct_is_imputed": {
    "description": "True if `complementary_feed_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `complementary_feed_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "adequate_diet_breastfed_pct_change_from_nfhs4": {
    "description": "adequate diet breastfed pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_breastfed_pct_change_pct_from_nfhs4": {
    "description": "adequate diet breastfed pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "adequate_diet_breastfed_pct_filled": {
    "description": "adequate diet breastfed pct — same as `adequate_diet_breastfed_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `adequate_diet_breastfed_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_breastfed_pct_is_imputed": {
    "description": "True if `adequate_diet_breastfed_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `adequate_diet_breastfed_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "adequate_diet_nonbreastfed_pct_change_from_nfhs4": {
    "description": "adequate diet nonbreastfed pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_nonbreastfed_pct_change_pct_from_nfhs4": {
    "description": "adequate diet nonbreastfed pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "adequate_diet_nonbreastfed_pct_filled": {
    "description": "adequate diet nonbreastfed pct — same as `adequate_diet_nonbreastfed_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `adequate_diet_nonbreastfed_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_nonbreastfed_pct_is_imputed": {
    "description": "True if `adequate_diet_nonbreastfed_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `adequate_diet_nonbreastfed_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "adequate_diet_total_pct_change_from_nfhs4": {
    "description": "adequate diet total pct — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_total_pct_change_pct_from_nfhs4": {
    "description": "adequate diet total pct — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "adequate_diet_total_pct_filled": {
    "description": "adequate diet total pct — same as `adequate_diet_total_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `adequate_diet_total_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "unknown",
    "cluster": "trend"
  },
  "adequate_diet_total_pct_is_imputed": {
    "description": "True if `adequate_diet_total_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `adequate_diet_total_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "stunting_pct_change_from_nfhs4": {
    "description": "% of children under 5 who are stunted (low height-for-age, chronic malnutrition) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "stunting_pct_change_pct_from_nfhs4": {
    "description": "% of children under 5 who are stunted (low height-for-age, chronic malnutrition) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "stunting_pct_filled": {
    "description": "% of children under 5 who are stunted (low height-for-age, chronic malnutrition) — same as `stunting_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `stunting_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "stunting_pct_is_imputed": {
    "description": "True if `stunting_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `stunting_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "wasting_pct_change_from_nfhs4": {
    "description": "% of children under 5 who are wasted (low weight-for-height, acute malnutrition) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "wasting_pct_change_pct_from_nfhs4": {
    "description": "% of children under 5 who are wasted (low weight-for-height, acute malnutrition) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "wasting_pct_filled": {
    "description": "% of children under 5 who are wasted (low weight-for-height, acute malnutrition) — same as `wasting_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `wasting_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "wasting_pct_is_imputed": {
    "description": "True if `wasting_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `wasting_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "severe_wasting_pct_change_from_nfhs4": {
    "description": "% of children under 5 who are severely wasted — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "severe_wasting_pct_change_pct_from_nfhs4": {
    "description": "% of children under 5 who are severely wasted — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "severe_wasting_pct_filled": {
    "description": "% of children under 5 who are severely wasted — same as `severe_wasting_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `severe_wasting_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "severe_wasting_pct_is_imputed": {
    "description": "True if `severe_wasting_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `severe_wasting_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "underweight_pct_change_from_nfhs4": {
    "description": "% of children under 5 who are underweight (low weight-for-age) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "underweight_pct_change_pct_from_nfhs4": {
    "description": "% of children under 5 who are underweight (low weight-for-age) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "underweight_pct_filled": {
    "description": "% of children under 5 who are underweight (low weight-for-age) — same as `underweight_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `underweight_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "underweight_pct_is_imputed": {
    "description": "True if `underweight_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `underweight_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "women_low_bmi_pct_change_from_nfhs4": {
    "description": "% of women 15-49 with BMI below normal (<18.5) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_low_bmi_pct_change_pct_from_nfhs4": {
    "description": "% of women 15-49 with BMI below normal (<18.5) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "women_low_bmi_pct_filled": {
    "description": "% of women 15-49 with BMI below normal (<18.5) — same as `women_low_bmi_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `women_low_bmi_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "women_low_bmi_pct_is_imputed": {
    "description": "True if `women_low_bmi_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `women_low_bmi_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "anaemia_children_pct_change_from_nfhs4": {
    "description": "% of children 6-59 months who are anaemic (<11.0 g/dl) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_children_pct_change_pct_from_nfhs4": {
    "description": "% of children 6-59 months who are anaemic (<11.0 g/dl) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "anaemia_children_pct_filled": {
    "description": "% of children 6-59 months who are anaemic (<11.0 g/dl) — same as `anaemia_children_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `anaemia_children_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_children_pct_is_imputed": {
    "description": "True if `anaemia_children_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `anaemia_children_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "anaemia_nonpregnant_women_pct_change_from_nfhs4": {
    "description": "% of non-pregnant women 15-49 who are anaemic (<12.0 g/dl) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_nonpregnant_women_pct_change_pct_from_nfhs4": {
    "description": "% of non-pregnant women 15-49 who are anaemic (<12.0 g/dl) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "anaemia_nonpregnant_women_pct_filled": {
    "description": "% of non-pregnant women 15-49 who are anaemic (<12.0 g/dl) — same as `anaemia_nonpregnant_women_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `anaemia_nonpregnant_women_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_nonpregnant_women_pct_is_imputed": {
    "description": "True if `anaemia_nonpregnant_women_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `anaemia_nonpregnant_women_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "anaemia_all_women_pct_change_from_nfhs4": {
    "description": "% of all women 15-49 who are anaemic — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_all_women_pct_change_pct_from_nfhs4": {
    "description": "% of all women 15-49 who are anaemic — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "anaemia_all_women_pct_filled": {
    "description": "% of all women 15-49 who are anaemic — same as `anaemia_all_women_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `anaemia_all_women_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_all_women_pct_is_imputed": {
    "description": "True if `anaemia_all_women_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `anaemia_all_women_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "anaemia_men_pct_change_from_nfhs4": {
    "description": "% of men 15-49 who are anaemic (<13.0 g/dl) — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; decrease is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_men_pct_change_pct_from_nfhs4": {
    "description": "% of men 15-49 who are anaemic (<13.0 g/dl) — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "anaemia_men_pct_filled": {
    "description": "% of men 15-49 who are anaemic (<13.0 g/dl) — same as `anaemia_men_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `anaemia_men_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "anaemia_men_pct_is_imputed": {
    "description": "True if `anaemia_men_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `anaemia_men_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  },
  "menstrual_hygiene_pct_change_from_nfhs4": {
    "description": "% of women 15-24 using hygienic menstrual protection methods — change from NFHS-4 state baseline to NFHS-5 district value (negative = decrease, positive = increase; increase is improvement)",
    "unit": "percent",
    "cluster": "trend"
  },
  "menstrual_hygiene_pct_change_pct_from_nfhs4": {
    "description": "% of women 15-24 using hygienic menstrual protection methods — relative % change from NFHS-4 state baseline to NFHS-5 district value",
    "unit": "percent_change",
    "cluster": "trend"
  },
  "menstrual_hygiene_pct_filled": {
    "description": "% of women 15-24 using hygienic menstrual protection methods — same as `menstrual_hygiene_pct`, but missing NFHS-5 values are filled with the NFHS-4 state baseline as a fallback. Check `menstrual_hygiene_pct_is_imputed` before treating a value here as a real district-level measurement.",
    "unit": "percent",
    "cluster": "trend"
  },
  "menstrual_hygiene_pct_is_imputed": {
    "description": "True if `menstrual_hygiene_pct_filled` is a fallback (NFHS-4 state baseline), not a real NFHS-5 district value — i.e. `menstrual_hygiene_pct` was missing for this district.",
    "unit": "boolean",
    "cluster": "trend"
  }
}